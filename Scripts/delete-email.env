# Copy to delete-email.env (keep out of git) and fill in REAL passwords (not the literal ...).
# Copy values from MicrohireAgentChat/appsettings.json → ConnectionStrings:BookingsDb / AppConnection
# (or appsettings.Development.Local.json if you use that).

export AITEST_HOST=tcp:116.90.5.144,41383
export AITEST_DB=AITESTDB
export AITEST_USER=PowerBI-Consult
export AITEST_PASSWORD='PASTE_BOOKINGSDb_PASSWORD_HERE'

export INTENT_HOST=tcp:intenttest.database.windows.net,1433
export INTENT_DB=IntentTestDB
export INTENT_USER=azadmin
export INTENT_PASSWORD='PASTE_APPCONNECTION_PASSWORD_HERE'
